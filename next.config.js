/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['ui-avatars.com', 'wallpaperaccess.com', 'www.transparenttextures.com'],
  },
}

module.exports = nextConfig
